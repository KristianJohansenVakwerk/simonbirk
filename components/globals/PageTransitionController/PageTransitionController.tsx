'use client';

const PageTransition = () => {
  // const { setGlob } = useStore((state) => state);

  // useEffect(() => {
  //   if (!params?.slug) return;

  //   // const scrollY = getGlobalScrollPosition(params?.slug as string);

  //   // window.scrollTo(0, scrollY as number);
  // }, [params]);

  return null;
};

export default PageTransition;
